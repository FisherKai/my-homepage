import type { MenuItem } from '../types/menu';

const menuList: Array<MenuItem> =[{
    name:'菜单0',
    path:'/',
    children:[],
    index: 0,
},{
    name:'菜单1',
    path:'/about',
    children:[],
    index: 1,
}];

export default menuList;